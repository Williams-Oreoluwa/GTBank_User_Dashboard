import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Gtbank_User_dashboard',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
